# Demo API base URL (no trailing slash). Defaults to http://127.0.0.1:8787
NEXT_PUBLIC_API_URL=http://127.0.0.1:8787

# Clerk authenticates every portal session. Use keys from the same instance as gridgo-api.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
